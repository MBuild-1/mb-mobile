import 'modal_dialog_controller.dart';

class SearchFilterModalDialogController extends ModalDialogController {
  SearchFilterModalDialogController(
    super.controllerManager
  );
}

class SearchFilterDelegate {}