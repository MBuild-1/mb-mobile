import 'search_last_seen_history.dart';

class ProductEntrySearchLastSeenHistory extends SearchLastSeenHistory {
  ProductEntrySearchLastSeenHistory({
    required super.id,
    required super.name,
    required super.slug,
    required super.imageUrl
  });
}